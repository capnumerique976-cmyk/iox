# Template pour smoke-authenticated.sh
# Copier en scripts/.iox-smoke.env (gitignoré) ou ~/.iox-smoke.env (chmod 600).
#
# Voir docs/ops/SMOKE-AUTH.md pour les détails.

BASE_URL=http://localhost:3000
SMOKE_EMAIL=admin@iox.mch
SMOKE_PASSWORD='Admin@IOX2026!'

# Timeout HTTP par appel, secondes (défaut 10)
# SMOKE_TIMEOUT=15
